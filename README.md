# PyTAF — Phase 0
uv-managed bootstrap with `src/` layout, echo transport, hash-locked constraints,
offline wheelhouse, and a working CLI (`pytaf init`). Targets Python 3.11 on
Windows 11 and Ubuntu 24.04. All installs use `uv`.
